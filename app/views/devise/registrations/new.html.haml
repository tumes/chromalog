- title "New user"

= header('Sign Up') do
  = simple_form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
    %p
      = f.label :email
      = f.text_field :email
      = f.error :email
    %p
      = f.label :name
      = f.text_field :name
      = f.error :name
    %p
      = f.label :password
      = f.password_field :password
      = f.error :password
      %br/
      = f.hint 'Minimum 4 characters'
    %p
      = f.label :password_confirmation
      = f.password_field :password_confirmation
      = f.error :password_confirmation
    = f.input :security_code
    / = f.input :terms_of_service, :as => :radio
    %p
      = f.label :terms_of_service
      By agreeing to the terms of service you are affirming that all records your keep in Chromalog are truthful and complete and that all submissions to Chromalog are equivalent to logs kept in your laboratory notebook.
      %br/
      = f.check_box :terms_of_service, {}, "true"
      I agree
      = f.error :terms_of_service
    = f.submit 'Sign up', :disable_with => "Submitting..."
