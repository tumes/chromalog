.indexitem{ :id => "#{"even" if run_log_counter.even?}"}
  %h1= "Run log for #{run_log.instrument.name} #{run_log.analytical_method.name}"
  = "#{pluralize(run_log.calibration_levels.count, "level")}, #{pluralize(run_log.calibration_levels.first.area_counts.count, "analyte")}"
